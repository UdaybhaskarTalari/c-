#include<iostream>
#include<vector>
using namespace std;
int main()
{
    vector<int>v1={10,20,30,40};
    vector<int>::iterator it=v1.begin();
    cout<<*(it)<<endl;
    //it++;
    it=it+2;
    cout<<*(it);
}
